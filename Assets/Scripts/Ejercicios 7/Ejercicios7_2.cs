using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicios7_2 : MonoBehaviour
{
    int numero = 100;
    // Start is called before the first frame update
    void Start()
    {
        for (int i = 1;i <= numero ;i++) {

            Debug.Log(i);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
