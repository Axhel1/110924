using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicios1_5 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Ocurre por que la el metodo Update se recarga cada vez que pasa un frame
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
