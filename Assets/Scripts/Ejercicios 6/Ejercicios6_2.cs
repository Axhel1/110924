using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicios6_2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //a) W || Y && X && W || Z
            //Respuesta=False

        //b) x && !Y && !X || !W && Y
            //Respuesta=True

        //c) (W || !Y) && x || Z
            //Respuesta=True

        //d) x && Y && W || Z || X
            //Respuesta=True

        //e) Y || !(Y || Z && W)
            //Respuesta=True   

        //f) IX && Y && (IZ || IX)
            //Respuesta=False
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
